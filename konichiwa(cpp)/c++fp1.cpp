//?------------------------------------------
//! title: hitarth (ghost)
//` date: 2023-05-03 11:50:53
//?------------------------------------------//
#include <cstdlib>
#include <iostream>
#include <vector>

using namespace std;
// focus of topic is no datatype , Type Modifiers and keywords in c++
int main() {
  // data types
  // how to declair a datatype in cpp
  // datatype = value;
  int a = 1;
  float b = 2.2;
  char c = 'c';
  double d = 24E8; // 24 x 10^8
  bool q = true;   // bool can only be true or faulse
                   // constants
  const int z = 3;
  // const (type) name = value
  // we cant change its value like a = 30;
  //  size and type Modifiers
  //  signed int 4 used for integers (equivalent to int)
  //  unsigned int 4 can only store positive integers
  //  short 2 used for small integers (range -32768 to 32767)
  //  long at least 4 used for large integers (equivalent to long int)
  //  long long int 8 used for very large integers (equivalent to long long
  //  int). unsigned long long 8 used for very large positive integers or 0
  //  (equivalent to unsigned long long int) long double 8 used for large
  //  floating-point numbers signed char 1 used for characters (guaranteed range
  //  -127 to 127) unsigned char 1 used for characters (range 0 to 255)

  // other data types
  //
  // Derived Data Types
  // These are the data types that are derived from fundamental (or built-in)
  // data types. For example, arrays, pointers, function, reference.

  // User-Defined Data Types

  // These are the data types that are defined by user itself.
  // For example, class, structure, union, enumeration, etc.

  // ***************keywords************

  // asm 	      double  	new	switch
  // auto  	    else	  operator  template
  // break 	    enum  	private  	this
  // case     	extern  protected throw
  // catch	    float	  public  	try
  // char	      for	    register	typedef
  // class	    friend 	return  	union
  // const      goto	  short	    unsigned
  // continue	  if	    signed  	virtual
  // default   	inline	sizeof  	void
  // delete  	  int	     static  	volatile
  // do   	    long  	struct  	while
  return 0;
}
